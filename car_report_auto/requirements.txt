# Web 框架
Flask==2.3.3

# 浏览器自动化
playwright==1.40.0

# HTTP 请求
requests==2.31.0

# 日志记录
loguru==0.7.2

# 数据处理
pydantic==2.5.0
psutil~=5.9.0
# Redis 支持
redis==5.0.1

# 异步支持（Python 3.9+ 内置，但为了兼容性保留）
# asyncio, json, datetime, threading, queue, os 都是 Python 标准库 